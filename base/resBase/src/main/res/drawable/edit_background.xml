<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 内部颜色 -->
    <solid
        android:color="@color/Mychat_Green" android:state_enabled="true" />

    <!-- 边缘线条颜色 -->
    <stroke
        android:width="0dp"
        android:color="@color/Surface" />
    <!-- 圆角的幅度 -->
    <corners
        android:topLeftRadius="@dimen/dp_16"
        android:topRightRadius="@dimen/dp_16"
        android:bottomLeftRadius="@dimen/dp_16"
        android:bottomRightRadius="@dimen/dp_16"/>
</shape>